services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
    volumes:
      - bizethiopia-data:/app/data
    restart: unless-stopped

volumes:
  bizethiopia-data:
